import { PostContainer } from 'entities/post';
import {FC} from 'react';

const MainPage:FC = () => {
    return (
        <div>
            <PostContainer/>
        </div>
    );
};

export default MainPage;